import { Component } from '@angular/core';

@Component({
  selector: 'git-cmp',
  templateUrl: './git.component.html'
})
export class GitComponent {
  // On Submit or On select trigger set SCM type variable
  // Based on that value, different components should be loaded
}
